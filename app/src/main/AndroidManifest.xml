<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mei_husky.samplemvvm">

    <application
        android:allowBackup="true"
        android:name=".common.MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.A01MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.activity.A02DataBindingBaseActivity"
            android:label="DataBindingBase" />

        <activity android:name=".view.activity.A03RecyclerBindActivity"
            android:label="列表展示"/>

        <activity android:name=".view.activity.A04MulTypeRecyclerBindActivity"
            android:label="多类型列表展示"/>

        <activity android:name=".view.activity.A06MvvmActivity"
            android:label="Mvvm"/>

        <activity android:name=".view.activity.A05MultiTypeStudyActivity"
            android:label="MultiType库学习"/>

    </application>

</manifest>